This is an automated message from the feedback system.

There is new feedback for course <%= @instance.course.code %> <%= @instance.course.name %>.

<%= PATH_PREFIX %><%= instance_root_path(@instance.course.code, @instance.path) %>
